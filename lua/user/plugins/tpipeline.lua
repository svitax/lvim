local M = {}

M.config = function()
  -- local status_ok, tpipeline = pcall(require, "tpipeline")
  -- if not status_ok then
  --   return
  -- end

  -- tpipeline.setup {}
  -- require("user.keymaps").set_textcase_keymaps()

  -- vim.g.tpipeline_autoembed = 0
  -- vim.opt.cmdheight = 0
  -- vim.opt.laststatus = 0
end

return M
